<div class="abea chat-card action-card">
    <header class="card-header flexrow">
        <img src="{{img}}" title="{{title}}" />
        <h3 class="item-name">{{title}}</h3>
    </header>

    <div class="card-content">
        <div class="card-result">{{note}}</div>
    </div>
</div>
